---
import type { ResolvedTag } from '@/types'

export interface Props {
  tags: ResolvedTag[]
}

const { tags } = Astro.props
---

<div class="flex flex-wrap gap-1.5 text-xs">
  {
    tags.map(({ tag }) => {
      return (
        <a
          class="font-mono text-[0.68rem] text-muted transition-colors duration-150 hover:text-accent"
          href={`/tags/${tag}/`}
        >
          #{tag}
        </a>
      )
    })
  }
</div>
