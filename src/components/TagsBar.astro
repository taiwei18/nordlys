---
import type { ResolvedTag } from '@/types'

export interface Props {
  tags: ResolvedTag[]
}

const { tags } = Astro.props
---

<div class="flex flex-wrap gap-x-6 gap-y-3 text-base">
  {
    tags.map(({ tag, icon }) => (
      <a
        class="clickable inline-flex items-center gap-1.5 no-underline"
        href={`/tags/${tag}/`}
      >
        <span class={`iconify shrink-0 text-xl leading-none ${icon}`} />
        <span class="leading-none font-normal">{tag}</span>
      </a>
    ))
  }
</div>
