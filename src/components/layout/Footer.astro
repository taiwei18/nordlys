---
import FooterItem from '@/components/layout/FooterItem.astro'
import Separator from '@/components/layout/Separator.astro'
import config from '@/theme.config'

const currentYear = new Date().getFullYear()
---

<footer class="mt-auto">
  <Separator />
  <div class="flex items-center justify-between gap-2 py-4 sm:py-8">
    <span class="text-base opacity-75">&#169; {currentYear} {config.author}</span>

    <div class="grow"></div>
    <div class="flex items-center gap-2">
      {config.footerItems.map((item) => <FooterItem {item} />)}
    </div>
  </div>
</footer>
